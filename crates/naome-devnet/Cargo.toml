[package]
name = "naome-devnet"
version.workspace = true
edition.workspace = true
description = "Bounded fixed-validator devnet provisioning and qualification tools."
license.workspace = true
repository.workspace = true
publish.workspace = true

[target.'cfg(unix)'.dependencies]
ed25519-dalek.workspace = true
getrandom = "0.3.4"
naome-chain = { path = "../naome-chain" }
naome-consensus = { path = "../naome-consensus" }
naome-network = { path = "../naome-network" }
naome-proof = { path = "../naome-proof" }
naome-storage = { path = "../naome-storage" }
rustix = { version = "1.1", features = ["fs"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2.workspace = true
zeroize = "1"

[lints]
workspace = true
