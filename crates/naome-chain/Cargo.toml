[package]
name = "naome-chain"
version.workspace = true
edition.workspace = true
description = "Authenticated artifact state and linear single-artifact blocks for NAOME."
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
naome-foundation = { path = "../naome-foundation" }
naome-ledger = { path = "../naome-ledger" }
naome-proof = { path = "../naome-proof" }
sha2.workspace = true

[dev-dependencies]
naome-checker = { path = "../naome-checker" }

[lints]
workspace = true
