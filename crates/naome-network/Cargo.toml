[package]
name = "naome-network"
version.workspace = true
edition.workspace = true
description = "Bounded authenticated artifact-chain transport and peer-address management for NAOME."
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
async-trait = "0.1.91"
getrandom = "0.3.4"
libp2p = { version = "0.56.0", default-features = false, features = [
    "ed25519",
    "macros",
    "noise",
    "request-response",
    "tcp",
    "tokio",
    "yamux",
] }
naome-protocol = { path = "../naome-protocol" }
naome-chain = { path = "../naome-chain" }
naome-consensus = { path = "../naome-consensus" }
naome-proof = { path = "../naome-proof" }
naome-storage = { path = "../naome-storage" }
sha2.workspace = true
tokio = { version = "1", default-features = false, features = ["macros", "time"] }

[dev-dependencies]
ed25519-dalek.workspace = true
naome-foundation = { path = "../naome-foundation" }
naome-ledger = { path = "../naome-ledger" }
naome-node = { path = "../naome-node" }
tokio = { version = "1", default-features = false, features = [
    "rt",
    "test-util",
] }

[lints]
workspace = true
